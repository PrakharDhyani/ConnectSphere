/**
 * Auth Routes — STUB
 * Full implementation comes in Phase 2 (JWT + Google OAuth)
 */
import { Router } from "express";

const router = Router();

// Placeholder — will be replaced with real controllers in Phase 2
router.get("/", (req, res) => {
  res.json({ message: "Auth routes — coming in Phase 2" });
});

export default router;
